# Go
/ralph
/ralph.exe
/dist/
/bin/
*.test
*.out
coverage.txt
go.work
go.work.sum

# GoReleaser
/dist/
/.goreleaser.yaml.bak

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Session/transient
.remember/

# Python (kept for reference/ tree)
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
_build/
.coverage
coverage.xml
coverage-*.xml
pytest-results-*.xml

# Generic logs
*.log

# Claude Code task-batch runtime state (scratch per-session)
.claude/state/

# comprehensive-review orchestrator scratch output
.full-review/

# Astro / site build artifacts
site/dist/
site/node_modules/
site/.astro/
site/pnpm-debug.log*

# Leftover binary from ad-hoc builds
/ralph

/radioactive_ralph

# fyne package build artifacts
*.app
*.dmg
*.exe
fyne-cross/

# Nested worktrees created inside the repo by mistake. Worktrees belong in
# ../.worktrees/, and a stray one here would otherwise be committed as content.
/.worktrees/

# Agent-state CACHE. directive.md and decisions.ndjson are the durable, tracked
# state; everything here is re-derivable and machine-local. The stop-hook
# fingerprint records a local HEAD sha and open-item count, so tracking it made
# every branch churn the same line (44 commits) and conflict with every other
# branch for no shared information.
.agent-state/.last-stop-fingerprint
.agent-state/.last-stop-sha
.agent-state/.last-compaction
.agent-state/.indexed-sha
.agent-state/digest.md
.agent-state/cursor.md
.agent-state/batch.json

# Ralph runtime state is user-level (XDG state root), never repo-committed.
# AGENTS.md: "There is NO committed repo state -- no .radioactive-ralph/ dir."
.radioactive-ralph/

# Per-turn provider result files. internal/provider/codex.go creates these in
# the project dir and removes them on cleanup, but a self-test run in flight
# leaves them present -- and `git add -A` then commits runtime state into a
# repo whose contract forbids exactly that.
.ralph-codex-result-*.txt

# Provider and acceptance scratch, created IN the project dir on purpose: a
# contained turn sets HOME/TMPDIR under the containment root so its writes
# cannot escape, and the orchestrator re-runs acceptance commands in a scratch
# tree of its own. A self-test in flight therefore leaves all of these behind.
#
# They must be ignored for `git add -A` to stay safe, which this repo requires.
# Their contents churn fast enough that add -A does not merely stage junk -- it
# FAILS mid-stat on a file the turn already deleted:
#
#   fatal: unable to stat '.codex-.../store.db': No such file or directory
#
# so an unignored pattern here breaks committing outright, not just cleanliness.
.codex-*
.rr-accept.*
.tmp-*

# Built by packaging/wsl/build-rootfs.sh -- a release artifact, not source.
packaging/wsl/rootfs.tar.gz
