---
title: Fixit advisor report — m3-completion
updated: 2026-04-15
status: current
domain: product
variant_recommendation: fixit
variant_alternate: green
confidence: 85
---

# Fixit advisor — m3-completion

Generated by `ralph run --variant fixit --advise` against `/Users/jbogaty/src/jbcom/radioactive-ralph` on branch `feat/m2-variant-service-cli`.

## Operator intent

**Goal:** finish M3: supervisor session pool, ralph run --detach via multiplexer, per-variant unique behavior. Self-host — use ralph to finish building ralph.

## Primary recommendation: `fixit-ralph`

plans/index.md does not exist (index_exists: false) and fixit is the only variant that can scaffold the required plans structure that gates all other variants for M3 completion.

## Alternate: `green-ralph`

Use the alternate when: Once plans/index.md exists and M3 tasks are scaffolded in .radioactive-ralph/plans/, switch to green for unlimited-loop autonomous execution of the supervisor session pool and multiplexer detach implementation.

## Tasks

- **[S/L]** Scaffold .radioactive-ralph/plans/index.md with required YAML frontmatter and M3 task references
- **[L/L]** Implement supervisor session pool in internal/supervisor/ with per-variant ClaudeSession lifecycle management
- **[M/L]** Implement ralph run --detach via internal/multiplexer/ using tmux/screen/Setsid fallback chain
- **[M/M]** Add per-variant bias snippet injection in internal/variant/ so each profile writes a distinct system-prompt segment
- **[M/M]** Wire internal/fixit/history.go and internal/fixit/refine.go into the six-stage pipeline defined in internal/fixit/pipeline.go

## Acceptance criteria

- [ ] .radioactive-ralph/plans/index.md exists with valid YAML frontmatter fields title, updated, and status
- [ ] go test ./internal/supervisor/... passes with ≥1 test covering session pool creation and teardown
- [ ] ralph run --detach returns exit code 0 and a running multiplexer PID exists in the XDG state directory
- [ ] grep -r 'BiasSnippet\|biasSnippet\|bias_snippet' internal/variant/*.go matches ≥1 result per non-default variant file
- [ ] go test ./internal/fixit/... passes with coverage ≥ 80% across pipeline.go, history.go, and refine.go
- [ ] go build -o dist/ralph ./cmd/ralph returns exit code 0 with no compiler errors

## Validation

All validation rules passed. This plan has `status: current` and will satisfy the plans-first gate for other variants.

## Methodology

This plan was produced by fixit-ralph's six-stage pipeline:
1. Operator intent capture (--description, --topic, constraints)
2. Deterministic repo exploration (git log, docs scan, PR/issue scrape, inventory)
3. Rule-based variant scoring (no LLM)
4. Constrained Claude analysis (sonnet subprocess, JSON-schema output)
5. Validation gates (variant exists, safety floors compatible, criteria measurable, files exist, confidence floor)
6. Plan emission (this file)

See `docs/design/fixit-plan-pipeline.md` for the pipeline contract.

## Refinement history

Fixit ran 2 refinement pass(es). Pass 2 was accepted.

### Pass 1  

- **Primary:** `fixit`
- **Confidence:** 82
- **Validation:** 1 failure(s)
  - acceptance criterion has no measurable verb: "Each variant's supervisor injects a distinct bias snippet verifiable via grep in internal/variant/*.go"

### Pass 2 ✓

- **Primary:** `fixit`
- **Confidence:** 85
- **Validation:** passed

