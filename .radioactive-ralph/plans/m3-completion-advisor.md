---
title: Fixit advisor report — m3-completion
updated: 2026-04-15
status: current
domain: product
variant_recommendation: fixit
variant_alternate: green
confidence: 85
---

# Fixit advisor — m3-completion

Generated by `radioactive_ralph run --variant fixit --advise` against `/Users/jbogaty/src/jbcom/radioactive-ralph` on branch `feat/m2-variant-service-cli`.

## Operator intent

**Goal:** finish M3: supervisor session pool, ralph run --detach via multiplexer, per-variant unique behavior. Self-host — use ralph to finish building ralph.

## Primary recommendation: `fixit-ralph`

No plans/index.md exists yet (plans.index_exists=false), and fixit is the only variant capable of scaffolding the plan infrastructure needed before any other variant can run — it scored highest at 60 with no disqualifications.

## Alternate: `green-ralph`

Use the alternate when: After fixit has created a passing plan and plans/index.md exists, green (score 50) is the best executor for the heavy implementation work: supervisor session pool, --detach wiring, and per-variant bias injection all require mirror-pool isolation and full write access.

## Tasks

- **[S/L]** Create .radioactive-ralph/plans/index.md with valid frontmatter and wire m3-completion plan reference
- **[L/L]** Implement supervisor session pool in internal/supervisor/ that spawns and manages claude subprocesses per variant profile
- **[M/L]** Wire --detach in cmd/ralph/run.go to use multiplexer.SpawnDetached instead of returning an error
- **[M/M]** Add config.Resolve() in internal/config/ that merges Daemon defaults, VariantFile overrides, and safety floors into a resolved Options struct
- **[M/L]** Implement per-variant bias injection in internal/variant/ that builds SystemPrompt snippets from installed skill inventory and profile preferences
- **[M/M]** Replace HandleAttach DB-polling with in-memory fan-out from the session pool for real-time event streaming
- **[M/M]** Add integration tests that spawn a real supervisor with fake-claude, exercise --detach lifecycle, and verify session pool scaling

## Acceptance criteria

- [ ] `radioactive_ralph run --variant green --detach` returns 0 and a tmux/screen session exists matching the expected session name pattern
- [ ] `radioactive_ralph status --variant green` returns JSON with ActiveSessions >= 1 after a --detach spawn
- [ ] config.Resolve() merges Daemon + VariantFile + safety floors and returns an error when floors are violated — passes unit tests with 100% branch coverage on the merge matrix
- [ ] `go test ./...` passes with 0 errors including new session pool and detach integration tests
- [ ] plans/index.md exists with valid YAML frontmatter and references the m3-completion plan file

## Validation

All validation rules passed. This plan has `status: current` and will satisfy the plans-first gate for other variants.

## Methodology

This plan was produced by fixit-ralph's six-stage pipeline:
1. Operator intent capture (--description, --topic, constraints)
2. Deterministic repo exploration (git log, docs scan, PR/issue scrape, inventory)
3. Rule-based variant scoring (no LLM)
4. Constrained Claude analysis (sonnet subprocess, JSON-schema output)
5. Validation gates (variant exists, safety floors compatible, criteria measurable, files exist, confidence floor)
6. Plan emission (this file)

See `docs/design/fixit-plan-pipeline.md` for the pipeline contract.
