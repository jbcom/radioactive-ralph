---
title: internal/projectid
description: Go API reference for the projectid package.
---

<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# projectid

```go
import "github.com/jbcom/radioactive-ralph/internal/projectid"
```

Package projectid computes a directory's project identity signals.

This lives outside internal/store on purpose. The signals are derived from the caller's own filesystem — an absolute path and two git facts — so a client may compute them without reading the supervisor\-owned database. Before this package existed, the only implementation lived in store, which forced every client that needed to identify its own directory to import store and therefore to open SQLite: exactly the dumb\-client violation issue \#204 is about.

The store still owns what these signals MEAN \(matching them against accumulated project identifiers\); this package only produces them.

## Index

- [Constants](<#constants>)
- [type Fingerprint](<#Fingerprint>)
  - [func Compute\(ctx context.Context, dir string\) \(\[\]Fingerprint, error\)](<#Compute>)


## Constants

<a name="KindAbsPath"></a>Fingerprint kinds. A project accumulates several over its life, so a directory that later gains a git remote still resolves to the same project.

```go
const (
    KindAbsPath       = "abs_path"
    KindGitRootCommit = "git_root_commit"
    KindGitRemote     = "git_remote"
)
```

<a name="Fingerprint"></a>
## type [Fingerprint](<https://github.com/jbcom/radioactive-ralph/blob/main/internal/projectid/projectid.go#L32-L35>)

Fingerprint is one identity signal for a project.

```go
type Fingerprint struct {
    Kind  string
    Value string
}
```

<a name="Compute"></a>
### func [Compute](<https://github.com/jbcom/radioactive-ralph/blob/main/internal/projectid/projectid.go#L41>)

```go
func Compute(ctx context.Context, dir string) ([]Fingerprint, error)
```

Compute returns the identity signals for dir, most stable last. The absolute path is always present; the git signals are added only when dir is inside a work tree, and a failure to read either one is not an error — a repo without an origin remote is perfectly normal.

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
